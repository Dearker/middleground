server:
    port: 8004
    servlet:
        context-path: /hanyi-daily

#日志級別
logging:
    level:
        root: info

spring:
    datasource:
        primary:
            driver-class-name: com.mysql.cj.jdbc.Driver
            jdbc-url: jdbc:mysql://localhost:3306/testdatasource1?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
            username: root
            password:
        secondary:
            driver-class-name: com.mysql.cj.jdbc.Driver
            jdbc-url: jdbc:mysql://localhost:3306/testdatasource2?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
            username: root
            password:
    http:
        encoding:
            charset: UTF-8
            enabled: true
            force: true

keji.id: 1
keji.name: 韩毅
keji.age: 20
